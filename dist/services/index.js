// Services layer placeholder for business logic and integrations
// e.g. Email service, Auth service, Cloud storage helper
export const UserService = {
    async getProfile(userId) {
        return { id: userId, mock: true };
    }
};
